<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ConvApp.FeedPage">
    <ContentPage.Content>
        <StackLayout>
            <StackLayout HorizontalOptions="Fill" BackgroundColor="blue">
                <Label Text="FeedPage" HorizontalOptions="CenterAndExpand" FontSize="Large" />
            </StackLayout>
            <ListView x:Name="list" VerticalOptions="Fill" BackgroundColor="Yellow">
                <!--<ListView.ItemTemplate>
                    <DataTemplate>
                        <ImageCell ImageSource="{Binding Image}"/>
                    </DataTemplate>
                </ListView.ItemTemplate>-->
            </ListView> 

        </StackLayout>
    </ContentPage.Content>
</ContentPage>