<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:ff="clr-namespace:FFImageLoading.Forms;assembly=FFImageLoading.Forms"
             x:Class="ConvApp.Login">
    <StackLayout BackgroundColor="#FFFFFF">
        <ff:CachedImage Source="resource://ConvApp.Resources.logo.jpg"
                        Aspect="AspectFill"
                        Margin="100"
                        HorizontalOptions="FillAndExpand"
                        VerticalOptions="FillAndExpand"/>
        <Label Text="EMAIL 1@paltoinfos.com (2,3도 있음) / PW 아무거나"
               FontSize="Medium"
               HorizontalOptions="Center"/>
        <Entry x:Name="emailEntry" Placeholder="Email"/>
        <Entry x:Name="pwdEntry" IsPassword="True" Placeholder="Password"/>
        <Button Text="Login" Clicked="Button_Clicked"/>
    </StackLayout>
</ContentPage>