<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ConvApp.PostEntryPage">
    <StackLayout x:Name="wrapper">

        <Image x:Name="image" HorizontalOptions="Center"/>
        <Entry x:Name="inputText" Placeholder="Share your experience here..."/>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Text="이미지 추가"
                    Clicked="OnImgAdd"/>
            <Button Text="저장"
                    Clicked="OnSave"
                    Grid.Column="1"/>
        </Grid>
    </StackLayout>
</ContentPage>
