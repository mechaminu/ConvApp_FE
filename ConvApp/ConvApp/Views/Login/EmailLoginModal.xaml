<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:ff="clr-namespace:FFImageLoading.Forms;assembly=FFImageLoading.Forms"
             xmlns:viewmodels="clr-namespace:ConvApp.ViewModels"
             xmlns:models="clr-namespace:ConvApp.Models"
             x:Class="ConvApp.EmailLoginModal">
  <ContentPage.Content>
        <StackLayout HorizontalOptions="FillAndExpand"
                     VerticalOptions="Center">
            <Frame BackgroundColor="White"
                   Margin="10"
                   Padding="10"
                   CornerRadius="10"
                   HasShadow="True">
                <StackLayout>
                    <Entry x:Name="idEntry" Placeholder="이메일 주소"/>
                    <Entry x:Name="pwdEntry" Placeholder="비밀번호" IsPassword="True"/>
                    <Button Text="로그인"
                            Clicked="Button_Clicked"/>
                    <BoxView HeightRequest="1"
                         HorizontalOptions="FillAndExpand"
                         BackgroundColor="Gray"
                         Margin="0,5,0,5"/>
                    <Button Text="회원가입"
                            Clicked="Button_Clicked_1"/>
                </StackLayout>
            </Frame>
        </StackLayout>
  </ContentPage.Content>
</ContentPage>