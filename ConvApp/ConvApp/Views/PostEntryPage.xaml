<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ConvApp.Views.PostEntryPage">
    <StackLayout x:Name="wrapper">
        <CarouselView x:Name="imageList" HeightRequest="400" BackgroundColor="Yellow">
            <CarouselView.ItemTemplate>
                <DataTemplate>
                    <Image Source="{Binding}"/>
                </DataTemplate>
            </CarouselView.ItemTemplate>
        </CarouselView>
        <Entry x:Name="inputTitle" Placeholder="포스트 제목"/>
        <Editor x:Name="inputText" Placeholder="포스트 내용"/>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Text="이미지 추가"
                    Clicked="OnImgAdd"/>
            <Button Text="저장"
                    Clicked="OnSave"
                    Grid.Column="1"/>
        </Grid>
    </StackLayout>
</ContentPage>
