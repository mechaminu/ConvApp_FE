<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:cntrols ="clr-namespace:ConvApp.Model"
             xmlns:local="clr-namespace:ConvApp.Views"
             x:Class="ConvApp.Views.RecipeContent">
    <StackLayout>
        <Button BackgroundColor="White" Text="게시" TextColor="Black" HorizontalOptions="End" Clicked="OnSave"/>

        <cntrols:CustomEditor1 x:Name="recipetitle" Text="" Placeholder="제목...."  FontSize="Body" FontAttributes="Bold" />
        
        <BoxView BackgroundColor="Gray" HeightRequest="1"/>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="100"/>
            </Grid.RowDefinitions>
            <cntrols:CustomEditor1 x:Name ="recipecontent"  Text="" Placeholder="레시피 내용"  FontSize="Body" FontAttributes="Bold"/>

        </Grid>
        <BoxView BackgroundColor="Gray" HeightRequest="1"/>
        <ListView x:Name="RecipeNode">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <local:RecipeContentViewCell/>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

        <BoxView BackgroundColor="Gray" HeightRequest="1"/>
        <Button Text="Camera" Clicked="cameraplus"/>

    </StackLayout>
</ContentPage>