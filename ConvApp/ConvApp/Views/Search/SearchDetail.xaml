<?xml version="1.0" encoding="utf-8" ?>
<TabbedPage xmlns="http://xamarin.com/schemas/2014/forms"
            xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
            xmlns:custom="clr-namespace:ConvApp.Utilites"
            x:Class="ConvApp.Views.SearchDetail"
            Shell.NavBarIsVisible="True"
            BarBackgroundColor="Beige"
            BarTextColor="Black"
            SelectedTabColor="Black"
            UnselectedTabColor="Gray">
    <Shell.TitleView >
        <ContentView>
            <StackLayout VerticalOptions="CenterAndExpand" >
                <custom:CustomSearchBar x:Name="searchBar"
                                        Placeholder="Search"
                                        CancelButtonColor="Red"
                                        PlaceholderColor="DimGray"
                                        TextColor="Black"
                                        HeightRequest="40"
                                        FontSize="18"
                                        HorizontalTextAlignment="Start"
                                        TextChanged="OnTextChanged_Search"/>
            </StackLayout>
        </ContentView>
    </Shell.TitleView>

    <ContentPage Title="Top" BackgroundColor="White">
        <ListView x:Name="searchResults" ItemsSource="{Binding SearchResults}" />
    </ContentPage>
    
    <ContentPage Title="items"/>
    <ContentPage Title="recipe"/>
    <ContentPage Title="chef"/>
</TabbedPage>